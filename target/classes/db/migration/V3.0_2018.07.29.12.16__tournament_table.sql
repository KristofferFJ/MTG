CREATE TABLE TOURNAMENT_EVENT (
  TOURNAMENT_EVENT_ID   INTEGER NOT NULL,
  PLAYER_ONE            INTEGER NOT NULL,
  PLAYER_TWO            INTEGER NOT NULL,
  PLAYER_ONE_ROUNDS     INTEGER,
  PLAYER_TWO_ROUNDS     INTEGER,
  PRIMARY KEY (TOURNAMENT_EVENT_ID)
);

ALTER TABLE TOURNAMENT_EVENT
  ADD CONSTRAINT FKtournamentplayers FOREIGN KEY (PLAYER_ONE) REFERENCES PLAYER;
ALTER TABLE TOURNAMENT_EVENT
  ADD CONSTRAINT FKtournamentopponent1 FOREIGN KEY (PLAYER_TWO) REFERENCES PLAYER;
